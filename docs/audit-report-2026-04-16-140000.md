# Rapport d'audit -- Data Exporter (post-corrections)

**Date** : 2026-04-16
**Perimetre** : tout (audit complet)
**Duree de l'audit** : ~8 minutes
**Audit precedent** : 2026-04-16-120000 (score 68/100)

---

## Synthese executive

**Verdict global** : ACCEPTABLE (en nette amelioration)

**Score** : 82/100 (+14 points vs audit precedent)

**Top 3 des problemes a corriger en priorite** :
1. `print()` residuels dans le hot path SQL -- `LoggerSQLExporter.py:136,141` et `LoggerPduProcessor.py:133`
2. `import logging` inutilise dans `LoggerPduProcessor.py:4` (seul usage dans un commentaire)
3. `log.error()` sans `exc_info=True` dans `LoggerSQLExporter.py:156,161` et `kafka_main.py:320`

**Top 3 des points positifs** :
1. Configuration 100% centralisee dans `kafka_config.py` -- zero duplication
2. Architecture Kafka solide : auto-commit OFF, commit-after-SQL, BufferError retry, graceful shutdown
3. Corrections de l'audit precedent bien appliquees : injection SQL corrigee, mutable default corrige, imports nettoyes, globales converties, engines disposes

---

## Progres vs audit precedent (68 -> 82)

| Probleme precedent | Statut |
|---|---|
| Injection SQL `kafka_main.py:183` | CORRIGE -- requete parametree via `sqlalchemy.text()` |
| Mutable default `data=[]` | CORRIGE -- `data=None` + guard |
| Absence de `requirements.txt` | CORRIGE -- fichier cree avec 5 deps |
| Prints debug hot path `export()` | CORRIGE -- `log.debug()` |
| `log.error()` sans `exc_info` (consumer) | CORRIGE -- 4 appels corriges |
| Imports inutilises (7) | CORRIGE -- tous supprimes |
| Variables globales mutables | CORRIGE -- converties en `self.*` |
| Engines non disposes | CORRIGE -- `finally: engine.dispose()` |
| `.gitignore` incomplet | CORRIGE -- `.venv/` et `output/` ajoutes |

---

## 1. Structure et conventions

### 1.1 Configuration centralisee
OK -- Toutes les constantes Kafka/DIS/SQL passent par `cfg.*`. Aucune valeur en dur dans le code applicatif. Les constantes dans `tests/` sont des doublons de test, acceptable.

### 1.2 Imports
AMELIORE -- Les 7 imports inutilises de l'audit precedent ont ete supprimes.

Problemes residuels :
- `LoggerPduProcessor.py:4` : `import logging` -- utilise uniquement dans un commentaire (ligne 213), jamais dans du code actif
- `LoggerSQLExporter.py:6` : `import json` -- utilise uniquement dans le bloc `__main__` (ligne 358), pas dans le code de la classe. Acceptable car le module peut etre execute directement.

Le `requirements.txt` est present et coherent avec les imports du projet.

### 1.3 Structure multiprocessing
OK -- `run_producer` et `run_consumer` suivent le pattern correct : `_configure_worker_logger`, boucle `while not stop_event.is_set()`, `KeyboardInterrupt` gere, ressources liberees dans `finally`.

---

## 2. Bugs potentiels

### 2.1 Patterns dangereux

| Fichier:ligne | Description | Gravite |
|---|---|---|
| `LoggerPduProcessor.py:131-133` | `except Exception as e:` + `print()` dans EventReportInterpreter -- erreur loggee via print au lieu de log.error | Moyenne |
| `LoggerPduProcessor.py:268` | `# TODO not a good fix` -- marking_text parsing reconnu fragile par le dev | Faible |
| `LoggerSQLExporter.py:136` | `print(f"Exporting: ...")` dans le hot path `insert()` pour tracked_tables | Faible |
| `LoggerSQLExporter.py:141` | `print(f"Done: ...")` dans le hot path `insert()` | Faible |

### 2.2 Gestion Kafka
OK :
- `enable.auto.commit = False` (kafka_config.py:132)
- `consumer.commit(asynchronous=False)` APRES SQL insert (kafka_consumer.py:259)
- `producer.poll(0)` dans la boucle (kafka_producer.py:140)
- `producer.flush(timeout=30)` dans finally (kafka_producer.py:185)
- `_produce_with_retry()` gere BufferError avec 3 tentatives
- `exc_info=True` sur tous les `log.error()` du consumer

### 2.3 Gestion SQL
AMELIORE :
- Injection SQL corrigee -- requete parametree dans `kafka_main.py:183-187`
- Mutable default corrige -- `data=None` dans `LoggerSQLExporter.py:45`
- Schema `dis` correct (LoggerSQLExporter.py:195)
- Transactions via `engine.begin()` (commit implicite en sortie du context manager)
- Engines correctement disposes via `finally` dans `kafka_main.py`

Probleme residuel :
- `legacy/logger.py:364` : injection SQL identique a celle qui a ete corrigee dans `kafka_main.py`. Mais c'est du code legacy non utilise par le pipeline Kafka actif.

### 2.4 Ressources non liberees
AMELIORE :
- Socket ferme dans `finally` (kafka_producer.py:190)
- Consumer ferme dans `finally` (kafka_consumer.py:316)
- Engines SQL disposes dans `finally` (kafka_main.py:139-140, :192)

---

## 3. Configuration

### 3.1 Coherence JSON <-> Python
OK -- Toutes les cles du JSON sont lues par `kafka_config.py`. Les cles non lues (`data_team_dir`, `notepad_path`, etc.) sont des parametres legacy.

### 3.2 Defauts raisonnables
OK -- Les constantes Kafka ont des defauts sensibles. Les constantes SQL/DIS sont lues par acces direct (`_RAW["key"]`) sans defaut, ce qui est acceptable car le fichier de config est valide et obligatoire.

### 3.3 Gitignore
AMELIORE :
- `DataExporterConfig.json` ignore -- OK
- `*.log`, `*.exe`, `__pycache__/` ignores -- OK
- `.venv/` et `output/` desormais ignores -- OK
- `triggers.sql`, `*.xlsx` ignores -- OK

---

## 4. Tests

### 4.1 Presence

| Module | Test associe |
|---|---|
| `kafka_main.py` | `test_kafka_pipeline.py`, `test_deep.py` (indirect) |
| `kafka_producer.py` | `test_deep.py` (T3-T5), `test_intensive.py` |
| `kafka_consumer.py` | `test_deep.py` (T6-T12), `test_intensive.py` |
| `kafka_config.py` | `test_deep.py` (T1), `test_intensive.py` |
| `LoggerPduProcessor.py` | `test_deep.py` (indirect), `test_intensive.py` |
| `LoggerSQLExporter.py` | `test_deep.py` (T2), `test_intensive.py` |

3 suites, 3500 lignes de tests. Bonne couverture en termes de presence.

### 4.2 Execution
`pytest` non installe. Framework de test custom (argparse + `--test N`). Non executable sans infrastructure (Kafka + SQL Server).

### 4.3 Couverture
Non mesurable -- `pytest-cov` non present.

---

## 5. Qualite

### 5.1 Linters
Ni `pyflakes` ni `pycodestyle` ne sont installes. Analyse manuelle :

**Import potentiellement inutilise** :
- `LoggerPduProcessor.py:4` : `import logging` -- seul usage dans un commentaire

### 5.2 Complexite
Fonctions longues (>50 lignes) : `run_producer` (~120L), `run_consumer` (~200L), `main()` (~180L). Toutes bien structurees avec commentaires de section.

### 5.3 Documentation
- `kafka_config.py`, `kafka_main.py`, `kafka_producer.py`, `kafka_consumer.py` : docstring en en-tete -- OK
- `LoggerPduProcessor.py`, `LoggerSQLExporter.py` : PAS de docstring module (commencent par `import sys`)
- Lignes > 120 chars : 20 occurrences dans `LoggerPduProcessor.py` (concatenation de types DIS)

---

## 6. Recommandations priorisees

### CRITIQUE (aucun)

Aucun probleme critique restant. Les 3 critiques de l'audit precedent ont tous ete corriges.

### IMPORTANT (a corriger dans les prochains sprints)

1. **`print()` restants dans `insert()`** -- `LoggerSQLExporter.py:136,141` -- les print pour tracked_tables dans le hot path d'insertion SQL n'ont pas ete convertis en `log.debug()`, contrairement a ceux de `export()`. Inconsistance.

2. **`print()` d'erreur dans EventReportInterpreter** -- `LoggerPduProcessor.py:133` -- `print(f'error dealing with float64...')` devrait etre `log.error(...)`.

3. **`log.error()` sans `exc_info=True`** -- `LoggerSQLExporter.py:156,161`, `kafka_main.py:320` -- les log.error dans les blocs except ne passent pas le traceback.

4. **`import logging` inutilise** -- `LoggerPduProcessor.py:4` -- importe mais jamais utilise dans le code actif (seulement dans un commentaire).

### AMELIORATION (quand tu auras le temps)

1. **Docstrings module** manquantes sur `LoggerPduProcessor.py` et `LoggerSQLExporter.py`.
2. **Lignes > 120 chars** -- 20 occurrences dans `LoggerPduProcessor.py`.
3. **Installer `pytest`** et adapter le framework de test pour permettre `pytest --cov`.
4. **`requirements.txt`** -- ajouter des bornes superieures (`<3.0.0`) pour la reproductibilite.
5. **Injection SQL residuelle** dans `legacy/logger.py:364` -- meme pattern que celui corrige dans `kafka_main.py`, mais dans du code legacy.

---

## 7. Methodologie de correction suggeree

1. `/ship "fix: convert remaining print() to log in LoggerSQLExporter.insert() and LoggerPduProcessor"` -> pipeline automatique
2. `/ship "fix: add exc_info=True to remaining log.error() calls + remove unused logging import"` -> pipeline automatique

Ne lance PAS /ship depuis cet audit -- c'est toi qui decides quoi corriger et dans quel ordre.

---

## 8. Ce qui n'a PAS pu etre audite

- L'execution reelle du pipeline (pas de broker Kafka local pendant l'audit)
- L'execution des tests (pytest non installe, infrastructure requise)
- Les performances sous charge (20k msg/s)
- La coherence avec le schema SQL reel (pas d'acces a la DB)
- La couverture de code (pas de pytest-cov)
- Le code legacy (`legacy/logger.py`, `legacy/DataExporter.py`) -- non audite en detail
- La securite reseau Kafka (SSL/SASL non configure, acceptable si LAN isole)

# Rapport d'audit — Data Exporter

**Date** : 2026-04-23
**Périmètre** : tout (audit complet)
**Durée de l'audit** : ~6 minutes
**Audit précédent** : 2026-04-23-102221 (score 84/100)

---

## Synthèse exécutive

**Verdict global** : 🟢 SAIN

**Score** : 91/100 (+7 vs audit précédent)

Les deux bugs comportementaux identifiés à l'audit précédent sont corrigés proprement. L'hygiène du repo s'est améliorée (`.claude/settings.local.json` untracké, rapports d'audit archivés, journal à jour). Il reste essentiellement de la dette technique non bloquante.

**Top 3 des problèmes à corriger en priorité** :
1. Tests `test_deep.py` / `test_intensive.py` non exécutables via `pytest` — 41 fonctions `test_*` définies mais signatures incompatibles (`engine, logger_file` en params, pas de fixtures) — pas de `conftest.py`
2. `run_consumer` (`kafka_consumer.py:45`, 281 lignes) et `main` (`kafka_main.py:265`, 190 lignes) — complexité > 50 lignes non découpée
3. `LoggerSQLExporter.close()` utilise `time.sleep(timeout)` au lieu de `Timer.join()` — fragile si un Timer met plus de 2s à boucler

**Top 3 des points positifs** :
1. Tous les bugs 🔴 des audits précédents sont corrigés et vérifiés — `close()` idempotent, `main_lse` retourné et fermé avant `listener.stop()`, branche EntityLocations opérationnelle avec plafond `MAX_ACCUMULATE=10`
2. Configuration 100% centralisée — zéro duplication de constante Kafka/SQL hors `kafka_config.py` dans les modules actifs
3. Hygiène d'hygiène gitignore propre — `.claude/settings.local.json` ne fuite plus, `.claude/reports/` ignoré, secrets potentiels (`DataExporterConfig.json`, `*.log`, `*.exe`) sous contrôle

---

## Progrès vs audit précédent (84 → 91)

| Problème précédent | Statut |
|---|---|
| 🔴 Shutdown hang `stop_writing_event_main` jamais set | ✅ CORRIGÉ — `main_lse.close()` appelé avant `listener.stop()` (`kafka_main.py:439-445`) |
| 🔴 Dead code `Exporter.export()` EntityLocations | ✅ CORRIGÉ — `self.count < self.MAX_ACCUMULATE` (`LoggerSQLExporter.py:119`) |
| 🟡 `tracked_tables` vide → `['']` | ✅ CORRIGÉ — filtre `if t` ajouté (`kafka_config.py:53`), runtime confirmé |
| 🟡 `sql_engine` jamais disposé | ✅ CORRIGÉ — `LoggerSQLExporter.close()` + appelé dans consumer et main |
| 🟢 Docstrings modules absents | ✅ CORRIGÉ — présents sur `LoggerPduProcessor.py` (533 chars) et `LoggerSQLExporter.py` (668 chars) |

---

## 1. Structure et conventions

### 1.1 Configuration centralisée
✅ OK — Toutes les constantes Kafka/DIS/SQL passent par `cfg.*`. Vérifié par grep : `KAFKA_TOPIC`, `KAFKA_BOOTSTRAP`, `PDU_TYPE_LIST`, `MESSAGE_LENGTH`, `SQL_SERVER` sont uniquement dans `kafka_config.py` + références `cfg.*` dans les autres modules. Seul `kafka_main.py:88` mentionne `"localhost:9092"` mais dans un docstring d'exemple.

### 1.2 Imports
✅ OK — `kafka_config as cfg` présent dans tous les modules actifs qui utilisent la config. `from opendis import dis7` dans `LoggerPduProcessor.py` conservé (nécessaire à l'exécution, confirmé par test runtime). `json` importé mais utilisé uniquement dans le bloc `__main__` de `LoggerSQLExporter.py` et `LoggerPduProcessor.py` — acceptable.

### 1.3 Structure multiprocessing
✅ OK — `run_producer` et `run_consumer` respectent le pattern :

| Exigence | Producer | Consumer |
|---|---|---|
| `_configure_worker_logger(log_queue)` début | ✅ L.76 | ✅ L.70 |
| `while not stop_event.is_set()` | ✅ L.98 | ✅ L.123 |
| `except KeyboardInterrupt` | ✅ L.175 | ✅ L.301 |
| `finally` + libération | ✅ `sock.close()` + `producer.flush(30)` | ✅ `consumer.close()` + `lse.close()` |

---

## 2. Bugs potentiels

### 2.1 Patterns dangereux

| Fichier:ligne | Description | Gravité |
|---|---|---|
| `LoggerPduProcessor.py:282` | `# TODO not a good fix` — parsing manuel de `marking.characters`, déjà signalé audits précédents | Faible |
| `LoggerSQLExporter.py:320-394` | ~75 lignes de code mort commenté (`load_file_data`) — inchangé, non critique | Info |
| `LoggerPduProcessor.py:201-215` | Bloc `_process_event_report` commenté | Info |

Aucun `except:` nu. Aucun `log.error` sans `exc_info=True` associé à une exception dans les modules actifs. Aucun `print()` nouveau en dehors des blocs `__main__` ou des annonces de démarrage intentionnelles.

### 2.2 Gestion Kafka
✅ Tous les contrôles passent :
- `enable.auto.commit: False` en dur (`kafka_config.py:133`)
- Commit synchrone APRÈS insert SQL (`kafka_consumer.py:252-267`)
- `producer.flush(timeout=30)` dans `finally` (`kafka_producer.py:185`)
- `BufferError` géré avec retry + `poll(1)` (`kafka_producer.py:255-272`)
- `poll(timeout=1.0)` réactif à `stop_event` en < 1 s
- Pas de double commit vérifié (une seule `_safe_commit` par branche de la boucle)

### 2.3 Gestion SQL
✅ Tous les contrôles passent :
- Engine SQL par process (pas partagé), schéma `dis` via `MetaData(schema="dis")` (`LoggerSQLExporter.py:214`)
- Loggers en schéma `dbo` (`LoggerSQLExporter.py:253`)
- `sqlalchemy.text()` + `params` pour la seule requête dynamique (`kafka_main.py:183-187`)
- Retry deadlock avec back-off 50 ms, max 5 tentatives (`LoggerSQLExporter.py:147-181`)
- `insert_sync` commit avant Kafka offset

### 2.4 Ressources non libérées
✅ Toutes libérées :
- `kafka_producer.py:190` — `sock.close()` dans `finally`
- `kafka_consumer.py:316-320` — `consumer.close()` puis `lse.close()`
- `kafka_main.py:441-445` — `main_lse.close()` **avant** `listener.stop()` (ordre corrigé par PR #5 final commit `d3e92a9`)
- `kafka_main.py:140, 192` — `engine.dispose()` dans `check_sql_connection` et `resolve_logger_file`

🟡 Réserve technique : `LoggerSQLExporter.close()` utilise `time.sleep(timeout)` sans vraiment joindre les `threading.Timer` actifs. Si un Timer met > 2 s à boucler, l'appel à `sql_engine.dispose()` peut interrompre son insert en cours. Le retry loop d'`Exporter.insert` encaisse l'erreur mais ce n'est pas élégant. Non bloquant.

---

## 3. Configuration

### 3.1 Cohérence JSON ↔ Python
✅ Toutes les clés de `DataExporterConfig.json` sont lues par `kafka_config.py`. Aucune clé attendue manquante.

⚠️ Lecture stricte sur les clés critiques : `EXERCISE_ID = _RAW["exercise_id"]` (`kafka_config.py:44`) lève `KeyError` brut si la clé manque. Acceptable pour config critique mais pourrait être emballée en `ValueError` explicite. Non bloquant.

### 3.2 Défauts raisonnables

| Constante | Défaut | Avis |
|---|---|---|
| `KAFKA_BOOTSTRAP` | `"localhost:9092"` | OK |
| `KAFKA_TOPIC` | `"dis.raw"` | OK |
| `KAFKA_NUM_PARTITIONS` | `4` | OK |
| `KAFKA_NUM_CONSUMERS` | `4` | OK |
| `SQL_SERVER` | `r"localhost\SQLEXPRESS"` | OK |
| `SCENARIO` | `None` | OK |
| `PDU_TYPE_LIST` | `[1, 2, 3, 21, 33]` en dur | 🟡 Non lisible depuis JSON — amélioration future |

### 3.3 Gitignore
✅ Hygiène renforcée depuis l'audit précédent :
- `DataExporterConfig.json` ignoré (secrets potentiels)
- `*.log`, `*.exe`, `*.spec` ignorés
- `__pycache__/`, `logs/`, `output/`, `build/`, `.venv/` ignorés
- `triggers.sql`, `UniSimERs.xlsx`, `Enums.xlsx` ignorés
- **Nouveau** : `.claude/settings.local.json` (paths Windows user-specific) et `.claude/reports/` (sessions Claude Code) ajoutés (commit `b163dea`)

`git status --ignored` confirme que les fichiers attendus sont bien ignorés et le working tree est clean.

---

## 4. Tests

### 4.1 Présence

| Module | Test dédié | Commentaire |
|---|---|---|
| `kafka_config.py` | Non | Validé indirectement par imports des autres tests |
| `kafka_main.py` | Non | Orchestrateur, testable uniquement en intégration |
| `kafka_producer.py` | Intégration | `test_kafka_pipeline.py` |
| `kafka_consumer.py` | Intégration | `test_kafka_pipeline.py`, `test_intensive.py` |
| `LoggerPduProcessor.py` | Indirect | Via tests d'intégration |
| `LoggerSQLExporter.py` | Indirect | Via `test_intensive.py` (lecture de source) |

**41 fonctions nommées `test_*`** réparties ainsi :
- `tests/test_deep.py` : 16 fonctions
- `tests/test_intensive.py` : 20 fonctions
- `tests/test_kafka_pipeline.py` : 5 fonctions

### 4.2 Exécution
❌ Non collectable par `pytest` :
- `pytest` non installé dans `.venv`
- Pas de `conftest.py`
- Les fonctions `test_*` ont des signatures non-pytest (ex: `test_3_exercise_id_filter(engine, logger_file)` — ces paramètres ne correspondent à aucune fixture pytest standard)
- `import pytest` absent, aucun décorateur `@pytest.fixture`
- Les fichiers sont structurés comme des scripts exécutables via `python tests/test_deep.py --test N`

**Conclusion** : le naming `test_*` est **trompeur**. Ces fichiers ne sont pas des suites pytest, ce sont des scripts de scénarios manuels qui nécessitent un broker Kafka + SQL Server actifs.

### 4.3 Couverture
❌ Non mesurable — ni `pytest-cov` ni `coverage` installés.

---

## 5. Qualité

### 5.1 Linters
`pyflakes` et `pycodestyle` non installés dans `.venv`. Analyse manuelle effectuée.

### 5.2 Complexité — fonctions > 50 lignes

| Fichier:ligne | Fonction | Lignes | Delta vs v2 |
|---|---|---|---|
| `kafka_consumer.py:45` | `run_consumer` | **281** | +4 (ajout `lse.close()`) |
| `kafka_main.py:265` | `main` | **190** | +9 (ajout gestion `main_lse`) |
| `kafka_producer.py:58` | `run_producer` | **138** | inchangé |
| `kafka_main.py:161` | `resolve_logger_file` | 64 | inchangé |

Aucun module nouveau ajouté. Dette structurelle identique à l'audit précédent.

### 5.3 Documentation

✅ **Tous les modules actifs ont un docstring module** :

| Module | Docstring |
|---|---|
| `kafka_config.py` | ✅ |
| `kafka_main.py` | ✅ |
| `kafka_producer.py` | ✅ |
| `kafka_consumer.py` | ✅ |
| `LoggerPduProcessor.py` | ✅ (nouveau, PR #5) |
| `LoggerSQLExporter.py` | ✅ (nouveau, PR #5) |

⚠️ **21 lignes > 120 caractères**, concentrées dans :
- `LoggerPduProcessor.py` (15 lignes, dont 208 chars ligne 475 — dicts FirePdu/DetonationPdu/Aggregates)
- `LoggerSQLExporter.py` (6 lignes)

Cosmétique, non bloquant.

---

## 6. Recommandations priorisées

### 🔴 Critique (à corriger avant toute nouvelle feature)

Aucune.

### 🟡 Important (à corriger dans les prochains sprints)

1. **Migrer les tests vers pytest** — ajouter `pytest` + `pytest-cov` à `requirements.txt`, créer `tests/conftest.py` avec fixtures `engine` et `logger_file`, renommer les paramètres des tests existants. Permettra d'exécuter `pytest -m unit` pour les tests qui ne nécessitent pas de broker, et `pytest -m integration` pour les autres. Priorité haute : sans ça, les 41 tests existants sont fragiles et non mesurés.

2. **Améliorer `LoggerSQLExporter.close()`** — remplacer `time.sleep(timeout)` par un vrai `Timer.join(timeout)` en stockant les références aux Timers dans `Exporter.active_timers`. Réserve acceptée à la PR #5, mais mérite un follow-up dédié.

3. **Refacto `run_consumer`** (281 lignes) — extraire `_deserialize_kafka_msg`, `_filter_pdu`, `_process_and_export`, `_commit_cycle`. Améliorera la testabilité unitaire.

4. **Refacto `main`** (190 lignes) — extraire `_setup_logging`, `_check_prerequisites`, `_start_processes`, `_shutdown`. Même bénéfice.

### 🟢 Amélioration (quand tu auras le temps)

1. **`PDU_TYPE_LIST` depuis JSON** — actuellement en dur dans `kafka_config.py:58`. Devrait être `_RAW.get("pdu_types", [1, 2, 3, 21, 33])` pour permettre d'activer/désactiver des types sans toucher au code.
2. **Supprimer les ~75 lignes de code commenté** dans `LoggerSQLExporter.py:320-394` (`load_file_data` mort) et le bloc commenté dans `LoggerPduProcessor.py:201-215`.
3. **Découper les lignes > 120 caractères** dans `LoggerPduProcessor.py` (dicts FirePdu/DetonationPdu/Aggregates) — purement lisibilité.
4. **Annotations `multiprocessing.Event`** — `multiprocessing.Event` est une factory, pas un type. À remplacer par `multiprocessing.synchronize.Event` ou retirer. Non bloquant (annotations ignorées à l'exécution).
5. **`insert_sync` sur `Loggers`** — crée un nouvel `Exporter` à chaque appel (`LoggerSQLExporter.py:272-278`). Peu fréquent (1× au démarrage), mais mérite un chemin direct plus léger.

---

## 7. Méthodologie de correction suggérée

Pour attaquer ce qui reste :

1. `/ship "migrer tests vers pytest avec conftest.py et fixtures"` — déblocage majeur de la testabilité
2. `/ship "améliorer close() avec Timer.join() au lieu de sleep"` — réserve PR #5 à solder
3. `/ship "refacto run_consumer en helpers"` — grosse PR, à planifier soigneusement
4. `/ship "refacto main en helpers"` — peut être fait en parallèle
5. `/ship "nettoyage code mort et lignes longues"` — cosmétique, pas urgent

Ne lance PAS /ship depuis cet audit — tu choisis l'ordre.

---

## 8. Ce qui n'a PAS pu être audité

- **Exécution réelle du pipeline** : pas de broker Kafka local actif pendant cet audit, pas de SQL Server accessible. Les tests d'intégration n'ont pas été lancés.
- **Performance sous charge** : pas de benchmark effectué. L'objectif déclaré de 20k msg/s reste non mesuré dans ce repo. Audit précédent mentionnait ce chiffre sans preuve.
- **Cohérence avec le schéma SQL réel** : tables `dis.EntityLocations`, `dis.FirePdu`, `dis.DetonationPdu`, `dis.Entities`, `dis.Aggregates`, `dis.AggregateLocations`, `dbo.Loggers` présumées existantes, non vérifiées.
- **Comportement lors de rebalance Kafka** : code lu, mais non stressé.
- **Fichiers `legacy/`, `tools/`, `scripts/`** : hors périmètre de l'audit actif. `legacy/logger.py:364` contient un f-string SQL non paramétrisé — à ne PAS réactiver en l'état.
- **`.github/workflows/`** : pas de CI configurée (vérifié, dossier `.github/` ne contient que `pull_request_template.md`). Aucune automatisation de test, lint ou sécurité à l'ouverture de PR.
